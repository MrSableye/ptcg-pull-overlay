import React from 'react';
import ReactDOM from 'react-dom';
import PullDisplay from './components/PullDisplay';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <PullDisplay />
  </React.StrictMode>,
  document.getElementById('root')
);
